<Window x:Class="EscolaApp.Views.CursoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Cadastro de Cursos"
        Height="350"
        Width="699">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="73*"/>
            <ColumnDefinition Width="87*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- FORMULÁRIO -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.ColumnSpan="2">

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Nome do Curso" />
                <TextBox Width="200"
                 Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Carga Horária" />
                <TextBox Width="120"
                PreviewTextInput="SoNumeros"
                 Text="{Binding CargaHoraria, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">

                <Button Content="Salvar"
            Width="80"
            Margin="0,0,5,0"
            Command="{Binding SalvarCommand}" />

                <Button Content="Editar"
            Width="80"
            Margin="0,0,5,0"
            Command="{Binding EditarCommand}" />

                <Button Content="Excluir"
            Width="80"
            Command="{Binding ExcluirCommand}" />
            </StackPanel>

        </StackPanel>

        <!-- GRID -->
        <DataGrid Grid.Row="1"
              ItemsSource="{Binding Cursos}"
              SelectedItem="{Binding CursoSelecionado}"
              AutoGenerateColumns="False" Grid.ColumnSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" />
                <DataGridTextColumn Header="Carga Horária" Binding="{Binding CargaHoraria}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</Window>
